<% provide(:title, 'New language') %>
<h1 class="title">New language</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@language) do |f| %>
      <%= render 'shared/error_messages', obj: @language %>

      <%= f.label :name %>
      <%= f.text_field :name, placeholder: 'Enter language name', autofocus: true, class: 'form-control'%>

      <%= f.submit "Create new language", class: "btn btn-primary" %>
    <% end %>
    <br>
    <p><%= link_to 'Cancel and return to index', languages_path %></p>
  </div>
</div>
