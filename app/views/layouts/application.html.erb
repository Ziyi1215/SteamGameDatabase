<!DOCTYPE html>
<html>
  <script>
    window.onresize=resizePaddingTop;
    resizePaddingTop();
function resizePaddingTop(){
  var navHeight = document.getElementById("nav");
  if( navHeight > "50px" ){
    document.getElementById("main").style.paddingTop = navHeight;
  }
    }
  </script>
  <head>
    <title>Cord Cutting Calculator</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <% render 'layouts/shim' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container" id="main" style="margin: auto;">
      <% flash.each do |message_type, message| %>
        <div style="margin-top: 5%; margin-bottom: -1%" class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
